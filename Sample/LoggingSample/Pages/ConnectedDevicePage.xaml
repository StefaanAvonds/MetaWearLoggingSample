<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:pages="clr-namespace:LoggingSample.Pages"
                       xmlns:viewModel="clr-namespace:LoggingSample.ViewModels"
                       x:Class="LoggingSample.Pages.ConnectedDevicePage"
                       x:TypeArguments="viewModel:ConnectedDeviceViewModel">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Label Text="{Binding Path=Status}" />

            <Button Text="Start logging" Command="{Binding Path=OnStartLogging}" />
        </StackLayout>
    </ContentPage.Content>
</pages:BaseContentPage>